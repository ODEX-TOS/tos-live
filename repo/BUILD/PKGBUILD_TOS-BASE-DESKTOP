# Maintainer: Tom Meyers

pkgname=tos-base-desktop
pkgver=5
pkgrel=1
pkgdesc='Package set for the tos desktop'
url='https://tos.odex.be'
arch=('any')
license=('MIT')
depends=(
    # base
    'tos-base'
    # WM/DE stuff
    'tde' 'st-tos' 'libinput-gestures-tos' 'arandr' 'nm-connection-editor' 'materia-gtk-theme' 'papirus-icon-theme' 'xcursor-human' 'xdotool'
    # DM
    'tde-greeter'
    'feh'
    # xorg
    'xorg-server' 'xorg-xbacklight' 'xorg-xclipboard' 'xorg-xinit' 'xorg-xprop' 'xorg-xinput' 'xorg-xrandr' 'xclip'
)


package() {
  # enable touch tapping for touchpads by default
    install -Dm644 /dev/stdin "$pkgdir/etc/X11/xorg.conf.d/10-touchpad.conf" <<EOF
Section "InputClass"
Identifier "touchpad"
Driver "libinput"
  MatchIsTouchpad "on"
  Option "Tapping" "on"
EndSection
EOF
}

# vim: ts=2 sw=2 et:

